spring.application.name=crmWebApp
# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/skillcapital
spring.datasource.username=myuser
spring.datasource.password=root
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.open-in-view=false
spring.datasource.driver-class-name=org.postgresql.Driver

#springdoc.packages-to-scan=com.example.crm


#spring.jackson.date-format=dd/MM/yyyy 
#spring.jackson.time-zone=IST



springdoc.packages-to-scan=user.com,lead.com,opportunity.com,learners.com,courses.com

#spring.main.allow-bean-definition-overriding=true


#spring.jpa.hibernate.ddl-auto=none

#spring.main.allow-circular-references=true
#logging.level.org.springframework.security=DEBUG


spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true


spring.jpa.open-in-view=true

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


# Spring Security (if using)
spring.security.user.name=
spring.security.user.password=

logging.level.org.springframework=DEBUG
logging.level.com.opportunity=DEBUG
server.error.include-message=always
server.error.include-stacktrace=always

# Swagger UI properties
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

# Base path for API documentation
#springdoc.api-docs.path=//v3//api-docs
#springdoc.swagger-ui.path=//swagger-ui.html

# Customizing Swagger UI
springdoc.swagger-ui.urls[0].name=User Management API
springdoc.swagger-ui.urls[0].url=/v3/api-docs
springdoc.swagger-ui.operations-sorter=method

# PostgreSQL Database Configuration

springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html





# Hibernate Configuration
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect  # Dialect for PostgreSQL
                         # 'update' creates or updates tables automatically
# Enable or disable serialization of null values (default is false)
#spring.jackson.default-property-inclusion=non_null

# Enable or disable FAIL_ON_EMPTY_BEANS (default is true)
spring.jackson.serialization.fail-on-empty-beans=false

# Configure the visibility of fields (e.g., public, getter, etc.)
spring.jackson.visibility.field=any
spring.jackson.visibility.getter=any
spring.jackson.visibility.is-getter=any
spring.jackson.visibility.setter=any

# You can also configure the default naming strategy (if needed)
# This setting applies only if you don't override it in your JacksonConfig class
spring.jackson.property-naming-strategy=SNAKE_CASE

